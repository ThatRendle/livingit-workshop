[project]
name = "livingit-workshop"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "passlib[bcrypt]",
]

[tool.black]
line-length = 88

[tool.mypy]
strict = true
