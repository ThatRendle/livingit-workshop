[project]
name = "notirc-client"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "textual>=0.80.0",
    "websockets>=12.0",
]

[project.scripts]
notirc = "notirc_client:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
